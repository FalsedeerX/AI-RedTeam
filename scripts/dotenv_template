###############################################
# Falsedeer's Unified Global Configuration V2 #
###############################################

# <<<<<<<<<<<<<<< Backend configruation >>>>>>>>>>>>>>>
DB_PORT=5432
DB_HOST=localhost
DB_NAME=airedteam
DB_SCHEMA=app

# master password (database owner)
DB_OWNER_USER=aesir
DB_OWNER_PASSWORD="pa$$w0rd"

# runtime user
DB_RUNTIME_USER=airedteam_runtime
DB_RUNTIME_PASSWORD=

# migration user
DB_MIGRATE_USER=airedteam_migrate
DB_MIGRATE_PASSWORD=

AGENT_SERVICE_URL=http://localhost:8100

DOCKER_SANDBOX_ENABLED=true

# <<<<<<<<<<<<<<< Clerk (backend JWT verification) >>>>>>>>>>>>>>>
# Copy these from the Clerk dashboard (https://dashboard.clerk.com → API Keys / JWT).
# CLERK_ISSUER is the Frontend API origin, e.g. https://<slug>.clerk.accounts.dev
# CLERK_JWKS_URL is <issuer>/.well-known/jwks.json
# CLERK_AUDIENCE is optional; set it only if you configured a custom JWT template "aud" claim.
CLERK_ISSUER=
CLERK_JWKS_URL=
CLERK_AUDIENCE=

# Dev-only fallback. Leave unset (or 0) in CI / staging / prod.
# When AUTH_DEV_BYPASS=1 *and* CLERK_ISSUER is empty, the backend accepts the legacy
# X-User-Id header so local tooling still works before Clerk env vars are configured.
AUTH_DEV_BYPASS=0

# <<<<<<<<<<<<<<< Frontend Configuration >>>>>>>>>>>>>>>
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Vite-visible (must be prefixed with VITE_)
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_CLERK_PUBLISHABLE_KEY=

# <<<<<<<<<<<<<<< Docker Sandbox Configuration >>>>>>>>>>>>>>>
# Set to "true" to run each agent scan inside an isolated Docker container.
# When enabled, the Backend creates a per-project Docker container running
# the full agent service (LangGraph + tools) with restricted networking.
DOCKER_SANDBOX_ENABLED=false
DOCKER_SANDBOX_IMAGE=redteam-agent:latest
DOCKER_MEM_LIMIT=2g
DOCKER_CPU_QUOTA=150000
# Additional IPs the container is allowed to reach (comma-separated).
# Auto-detected for Ollama and MSF; add extras here if needed.
DOCKER_ALLOWED_HOSTS=
